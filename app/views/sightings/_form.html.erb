<%= form_for(@sighting) do |f| %>
  <% if @sighting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sighting.errors.count, "error") %> prohibited this sighting from being saved:</h2>

      <ul>
      <% @sighting.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="row">
    <div class="col-md-6">
      <div class="field">
        <%= f.label :date %><br>
        <%= f.date_select :date %>
      </div>
      <div class="field">
        <%= f.label :time %><br>
        <%= f.time_select :time %>
      </div>

      <div class="field">
        <%= select(:sighting, :region, @regions_for_select)%>
      </div>

      <div class="field">
        <%= select(:sighting, :animal_id, @animals_for_select)%>
      </div>

      <div class="field">
        <%= f.label :lat_long %><br>
        <%= f.text_field :lat_long %>
      </div>

      <div class="actions">
        <%= f.submit class:"btn btn-default" %>
      </div>
  </div>

    <div class="col-md-6">
      <img class="burd" src="https://hd.unsplash.com/photo-1462953382322-d349274e1165">
    </div>
  </div>
<% end %>
